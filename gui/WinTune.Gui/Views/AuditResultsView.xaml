<UserControl x:Class="WinTune.Gui.Views.AuditResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:WinTune.Gui.ViewModels"
             xmlns:ui="http://schemas.modernwpf.com/2019">
  <UserControl.DataContext>
    <vm:AuditResultsViewModel />
  </UserControl.DataContext>

  <Grid Margin="32">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal">
      <TextBlock Text="Audit Results" FontSize="24" FontWeight="SemiBold" />
      <Button Content="&#xE72C; Run Audit" FontFamily="Segoe MDL2 Assets"
              Width="140" Height="36" Margin="20,0,0,0"
              Style="{ui:ThemeResource AccentButtonStyle}"
              Click="OnRunAuditClick" />
    </StackPanel>

    <!-- Score bar -->
    <Grid Grid.Row="1" Margin="0,16,0,20">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Border Width="80" Height="80" CornerRadius="40"
              Background="{ui:ThemeResource SystemAccentColorDarkBrush}"
              VerticalAlignment="Center">
        <TextBlock Text="{Binding Score}" FontSize="28" FontWeight="Bold"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Foreground="White" />
      </Border>
      <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0">
        <TextBlock FontSize="15" Text="Debloat Score" FontWeight="SemiBold" />
        <TextBlock FontSize="13" Foreground="{ui:ThemeResource TextFillSecondaryBrush}" Margin="0,4,0,0">
          <Run Text="{Binding Present}" FontWeight="SemiBold" />
          <Run Text=" items present out of " />
          <Run Text="{Binding Total}" FontWeight="SemiBold" />
        </TextBlock>
      </StackPanel>
    </Grid>

    <!-- Loading -->
    <ui:ProgressRing Grid.Row="2" IsActive="{Binding IsLoading}" Width="32" Height="32"
                     VerticalAlignment="Top" Margin="0,16"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}" />

    <!-- Error -->
    <Border Grid.Row="2" VerticalAlignment="Top"
            Background="#482222" CornerRadius="8" Padding="16,12"
            Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Text="&#xE783;" FontFamily="Segoe MDL2 Assets" FontSize="16"
                   Foreground="#EA3943" VerticalAlignment="Center" Margin="0,0,12,0" />
        <TextBlock Grid.Column="1" Text="{Binding Error}" VerticalAlignment="Center" />
      </Grid>
    </Border>

    <!-- Results -->
    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
      <ListView ItemsSource="{Binding Rows}" Margin="0,8,0,0"
                BorderThickness="0" Background="Transparent">
        <ListView.ItemTemplate>
          <DataTemplate>
            <Grid Height="36">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100" />
              </Grid.ColumnDefinitions>
              <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="16"
                         VerticalAlignment="Center" Margin="0,0,12,0"
                         Foreground="{ui:ThemeResource SystemAccentColorBrush}" />
              <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center"
                         FontSize="13" />
              <Border Grid.Column="2" CornerRadius="4" Padding="6,2"
                      Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}">
                <TextBlock Text="{Binding Type}" FontSize="11"
                           HorizontalAlignment="Center"
                           Foreground="{ui:ThemeResource TextFillSecondaryBrush}" />
              </Border>
            </Grid>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </ScrollViewer>
  </Grid>
</UserControl>
