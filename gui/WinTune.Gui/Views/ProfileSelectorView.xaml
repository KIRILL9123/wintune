<UserControl x:Class="WinTune.Gui.Views.ProfileSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:WinTune.Gui.ViewModels">
  <UserControl.DataContext>
    <vm:ProfileSelectorViewModel />
  </UserControl.DataContext>

  <Grid Margin="32">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Text="Profiles" FontSize="24" FontWeight="SemiBold" />
    <TextBlock Grid.Row="1" Text="Select a profile to audit, apply, or customize."
               FontSize="13" Foreground="{ThemeResource TextFillSecondaryBrush}"
               Margin="0,4,0,24" />

    <ProgressRing Grid.Row="2" IsActive="{Binding IsLoading}"
                  Width="32" Height="32" Margin="0,16" VerticalAlignment="Top"
                  Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />

    <Border Grid.Row="2" VerticalAlignment="Top" Background="#482222"
            CornerRadius="8" Padding="16,12"
            Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <FontIcon Glyph="&#xE783;" FontSize="16" Foreground="#EA3943"
                  VerticalAlignment="Center" Margin="0,0,12,0" />
        <TextBlock Grid.Column="1" Text="{Binding Error}" VerticalAlignment="Center" />
      </Grid>
    </Border>

    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
      <ItemsControl ItemsSource="{Binding Profiles}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <UniformGrid Columns="2" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border MinHeight="160" Margin="0,0,8,8" Padding="24,20"
                    CornerRadius="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="*" />
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal">
                  <TextBlock Text="{Binding Name}" FontSize="17" FontWeight="SemiBold" />
                  <Border CornerRadius="4" Padding="6,2" Margin="12,0,0,0"
                          Background="{ThemeResource SystemFillColorCriticalBrush}"
                          Visibility="{Binding Dangerous, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="DANGEROUS" FontSize="10" FontWeight="SemiBold" Foreground="#EA3943" />
                  </Border>
                </StackPanel>

                <TextBlock Grid.Row="1" Text="{Binding Description}" Margin="0,10,0,0"
                           Foreground="{ThemeResource TextFillSecondaryBrush}"
                           TextWrapping="Wrap" FontSize="13" MaxHeight="80" />

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,14,0,0">
                  <TextBlock FontSize="12" Foreground="{ThemeResource AccentFillColorDefaultBrush}">
                    <Run Text="{Binding TweakCount}" FontWeight="SemiBold" />
                    <Run Text=" tweaks" />
                  </TextBlock>
                  <Button Content="Select" Width="80" Height="28"
                          Margin="12,0,0,0" VerticalAlignment="Center"
                          x:Name="SelectBtn" Tag="{Binding Name}" />
                </StackPanel>
              </Grid>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </Grid>
</UserControl>
