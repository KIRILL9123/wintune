<UserControl x:Class="WinTune.Gui.Views.ProfileSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:WinTune.Gui.ViewModels">
  <UserControl.DataContext>
    <vm:ProfileSelectorViewModel />
  </UserControl.DataContext>

  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Text="Profiles" FontSize="28" FontWeight="SemiBold" />

    <ItemsControl Grid.Row="1" ItemsSource="{Binding Profiles}" Margin="0,24,0,0">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <WrapPanel />
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Width="260" Height="140" Margin="0,0,16,16" Padding="16" Background="#121826" CornerRadius="12">
            <StackPanel>
              <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="SemiBold" />
              <TextBlock Text="{Binding Description}" Margin="0,8,0,0" Foreground="#8B949E" TextWrapping="Wrap" />
              <Button Content="Select" Width="90" Margin="0,12,0,0" />
            </StackPanel>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </Grid>
</UserControl>
